name: silhouette_cli
description: A Svelte-like compiler for Dart that compiles templates into reactive Dart components using runes for state management
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  petitparser: ^6.0.0
  path: ^1.8.0
  args: ^2.4.0
  meta: ^1.9.0
  web: ^1.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
